# Progress Narrative

An npm package and interactive gallery for turning structured agent events into readable, animated operational updates.

## Development

```bash
npm install
npm run dev
```

## Verification

```bash
npm test
npm run pack:package
```

The publishable package is in `packages/progress-narrative`. Publication is intentionally handled separately from deployment of the gallery.
